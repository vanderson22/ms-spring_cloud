#Nome da applicação
spring.application.name=hr-worker

# conhecer a localização do config server
spring.cloud.config.uri=http://localhost:8888

# SEM NADA VAI NO PROFILE DEFAULT

  # COM PROPRIEDADE para habilitar profile = test
spring.profiles.active=test